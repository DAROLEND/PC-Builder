# Як я використовував AI

> ⚠️ **Перед публікацією:** розділи 3 і 4 має заповнити автор після власного рев'ю коду. Розділ 2 — фактичний журнал сесії генерації, у ньому нічого не вигадано.

## 1. Що делеговано

Проєкт згенеровано за допомогою **Claude Code** за моєю специфікацією: предметна область, стек, вимоги до валідації, permissions, агрегацій, фонових задач, інтеграцій, інфраструктури та AI-порадника.

Claude Code написав:
- модель даних і рушій сумісності з 13 групами правил;
- DRF API, permissions, фільтри, агрегації, OpenAPI-схему;
- Celery-задачі, інтеграції з НБУ, Stripe, Claude API;
- фронтенд на React + TypeScript + TanStack Query;
- тести, Docker Compose, CI, Render Blueprint, цей README.

Як перевірялося під час генерації (не «на око»):
- 160 backend-тестів на **справжньому PostgreSQL 17**, а не на SQLite, бо JSONB, GIN, віконні функції та `select_for_update` на SQLite поводяться інакше або відсутні;
- `ruff`, `makemigrations --check`, генерація OpenAPI з `--fail-on-warn`;
- `tsc`, `oxlint`, Vitest, production-збірка фронтенду;
- запуск живих серверів і наскрізний сценарій через HTTP: реєстрація → збірка → замовлення → оплата → порадник;
- прохід у справжньому браузері (Playwright + Edge) зі скриншотами й перевіркою консолі на помилки;
- реальний виклик API НБУ.

## 2. Де Claude помилився і як це знайшли

Нижче всі помилки, знайдені під час генерації. Колонка «Як знайшли» важлива: більшість з них не впала б сама по собі, їх виловив конкретний механізм перевірки.

| # | Помилка | Як знайшли | Виправлення |
|---|---|---|---|
| 1 | **Задача скасування неоплачених замовлень могла скасувати вже оплачене.** Вона вибирала `pending`-замовлення без блокування, і якщо між SELECT і `select_for_update` приходив вебхук оплати, перехід `paid → cancelled` (легальний для користувача) проходив. | Рев'ю власного коду до запуску тестів | Параметр `expected=` у `change_status`: перевірка очікуваного статусу *під блокуванням*. Регресійний тест, що імітує гонку. |
| 2 | **Оплата повертала 500, хоча замовлення вже оплачене.** Лист-нотифікація ставився в чергу в `on_commit`; без Redis `.delay()` кидав виняток уже після коміту. | **Лише запуск справжнього сервера без Redis.** У тестах Celery працює в eager-режимі, тож вони цього не бачили. | `enqueue_best_effort()`: збій брокера для необов'язкових задач логується, але не ламає запит. Регресійний тест із «падаючим» брокером. |
| 3 | **Гроші в двох форматах:** `total_price` у списку був рядком `"735.00"`, у деталях числом `735.0`. `SerializerMethodField`, що повертає `Decimal`, DRF рендерить як float. | **Згенеровані TypeScript-типи:** в одному місці `string`, в іншому `number` | `DecimalField` скрізь, контрактний тест на відрендерений JSON. |
| 4 | Анотація `kind` конфліктувала з `@property Component.kind`, а `values(name=…)` з полем моделі (ValueError під час виконання). | Падіння тестів статистики | Перейменування анотацій. |
| 5 | **AI-планувальник перевищував бюджет і витрачав залишок не туди:** жадібне «найдорожче в межах частки + перенесення залишку» віддавало гроші на кулер і SSD замість GPU, а дорогий CPU на AM5 «з'їдав» гроші на обов'язкові слоти. | Параметризовані тести (4 сценарії × 5 бюджетів) і ручний аналіз результатів | Поділ на «продуктивні» та «достатні» слоти, резерв під мінімальні ціни незаповнених слотів, апгрейд/даунгрейд, повне перепланування (локальний даунгрейд не може змінити платформу AM5 → AM4). |
| 6 | `bulk_update()` не оновлює `auto_now`-поле `updated_at`. | Рев'ю | Явне присвоєння в задачі синхронізації цін. |
| 7 | Помилка схеми OpenAPI: `get_queryset` замовлень падав на `AnonymousUser` під час генерації схеми. | `spectacular --fail-on-warn` | `swagger_fake_view`-гард. |
| 8 | `openapi-typescript` підтримує лише TypeScript 5, а шаблон Vite поставив TS 6. | Помилка `npm install` (ERESOLVE) | Зафіксовано TS 5.9, а не `--legacy-peer-deps`, який лише сховав би несумісність. |
| 9 | Передзаповнення форми конфігуратора через `setState` в `useEffect` (зайві каскадні рендери). | `oxlint` (`react/set-state-in-effect`) | Сторінка вантажить дані, а редактор отримує початковий стан через props і `key`. |
| 10 | `mutateAsync` без `catch` (необроблений reject при помилці збереження). | Рев'ю | `mutate` з `onSuccess`, помилки рендеряться з `mutation.error`. |
| 11 | Мобільна шапка розвалювалась у стовпчик. | Скриншот на 390 px | Навігація окремим рядком із горизонтальним скролом. |
| 12 | Помилки **в тестах** (не в коді): межове значення 351 мм (це 9 мм запасу, тобто таки «тісно»); очікування PSU-фільтра суперечило задокументованому контракту; тест «$700 на ігровий ПК» був нереалістичним, бо найдешевша сумісна збірка з GPU в каталозі коштує $715; фейковий клієнт Anthropic зберігав посилання на мутабельний список повідомлень. | Падіння тестів і аналіз, хто неправий: тест чи код | Виправлено тести. Для недосяжного бюджету додано окремий тест, що це чесно позначається. |
| 13 | **Тести залежали від сід-файлу**, який тепер оновлюється живими цінами: коли пам'ять подорожчала, «бюджетні» тести почали падати. | Падіння 28 тестів після першого оновлення цін | Окрема заморожена фікстура `tests/fixtures/catalog.json`; очікування, які можна обчислити з даних, більше не вписані жорстко. |
| 14 | Заглушка hotline «немає фото» (`/public/i/img-265.gif`) зберігалась як фото товару. | Скриншот каталогу | Фільтр заглушок у парсері + тест. |
| 15 | Правило «ціну беремо лише з пропозицій у наявності» лишало стару ціну з сіду, а поруч показувалось «від 3 139 грн». | Скриншот каталогу | Перевага в наявності, інакше остання відома ринкова ціна; тест на обидва випадки. |
| 16 | «1 пропозицій», «4 пропозицій»: немає узгодження числівників. | Скриншот | `Intl.PluralRules` у i18n + тест. |
| 17 | Спершу спробував отримувати дані через приватний API rozetka: він віддав лише ID без цін, а його robots.txt забороняє пошук. | Ручна перевірка | Відмовився від цього шляху на користь schema.org-розмітки сторінок товарів (див. README «Живі ціни й фото»). |
| 18 | Користувач помітив, що частина фото не вантажиться: оригінали на CDN агрегатора важать до 700 КБ, а для деяких клієнтів CDN відповідає 503. | Скриншот від користувача, потім перевірка CDN curl'ом з різними User-Agent | Фото дзеркаляться воркером у WebP (360 / 1000 px) і віддаються з `/media`; у dev-проксі Vite і nginx додано `/media`. |
| 19 | У каталозі було лише 65 вручну підібраних деталей (9 відеокарт), і картку не можна було відкрити. | Питання користувача | Імпорт категорій з рівнем «лише каталог» для деталей з неповними характеристиками; сторінка деталі з галереєю, усіма характеристиками й історією ціни. |
| 20 | Перший прогін імпорту на живих даних: (а) корпуси, процесори й плати не створювались узагалі, бо Django вважає порожній `specs = {}` «blank» у `full_clean`; (б) у категорії кулерів hotline тримає корпусні вентилятори, а в SSD — зовнішні USB-диски; (в) довгі назви пам'яті після обрізання до 64 символів давали однаковий SKU. | Лог імпорту на реальних сторінках (у тестах із фікстурами (а) і (в) не відтворювались: там були короткі назви з непорожніми specs) | Виключення `specs` з перевірки полів (`clean()` лишився), фільтри за модельними лінійками, унікальний SKU з суфіксом; на кожен випадок є тест, зайві записи прибрано з бази. |
| 21 | Фото «з'їжджали» й обрізались. Плитка фото — grid з рядком `auto`, тож `height: 100%` у картинки не розраховувався, і портретні фото брали природну висоту та обрізались. Поки були квадратні оригінали hotline, цього не було видно. | Скриншот від користувача, перевірка самого WebP-файлу (фото ціле) | Картинка прибита до країв плитки (`position: absolute; inset: 0`). |
| 22 | Правило кулера порівнювало його розсіювання з **базовим** TDP. i7-14700K — «125 Вт», але під навантаженням до 253 Вт, тож кулер на 150 Вт проходив без жодного попередження. | Питання користувача «чому немає попередження» | Потужність під навантаженням: Intel `max_power_w` (з таблиці hotline або даних Intel), для AMD — PPT = 1,35 × TDP. Нові попередження `COOLER_LOW_HEADROOM`, `COOLER_REQUIRED`, `COOLER_RATING_UNKNOWN` з рекомендованою потужністю. |
| 23 | Попередній SQL-фільтр ховав усі рідинні кулери, якщо корпус не вказує підтримку радіатора (`.get("max_radiator_mm", 0)`), хоча рушій у цьому випадку лише попереджає. Порушувався контракт «фільтр ніколи не ховає валідну деталь». Також правило кулера падало з `KeyError` на кулері без висоти. | Читання коду під час переходу на неповні дані з джерела | Невідомі розміри лишаються в списку, рушій попереджає (`COOLER_HEIGHT_UNKNOWN`); тести. |
| 24 | Перша версія «зміни ціни за 30 днів» показала Ryzen 5 3600 «−75 %», DeepCool PN650D «+82 %». Дані hotline справжні, але на ринку з одним-двома продавцями середня стрибає разом із ними. | Перевірка топу результатів вручну перед тим, як показувати бейджі | Тижневі медіани, мінімум 5 магазинів, зміни понад ±40 % не показуються; тести на кожне правило. |
| 25 | Тексти сповіщень: відсоток виходив «1E+1%» (`Decimal.normalize()`), а для збірки — «подешевшава» замість «подешевшала». | Тести на `render()` з реальними числами й обома родами | `percent_text()` («10», «7,5»), закінчення з окремим параметром для роду. |
| 26 | Випадна панель «Стежити за ціною» ховалась під блоком характеристик: верхній блок анімується (`transform`), тож він окремий шар, і `z-index` панелі не виходив за його межі. Кнопка виглядала робочою, але клік потрапляв у блок нижче. | Playwright: клік падав з «section.panel intercepts pointer events» | `z-index` для всього верхнього блоку сторінки. |

Висновок з таблиці: юніт-тести самі по собі не впіймали б пунктів 2, 3, 11, 14, 15 і 16. Їх знайшли інші механізми: запуск у середовищі, близькому до реального, генерація типів із контракту, погляд на реальний UI.

## 3. Що я перевірив і змінив сам

> _Заповни після рев'ю. Приклади того, що варто перевірити й описати:_
> - _Чи правильні дані в `catalog_seed.json` (сокети, чипсети, довжини відеокарт) — звір кілька позицій із сайтами виробників._
> - _Чи згоден ти з порогами: запас БЖ 30 %, «тісно» < 10 мм, оцінка споживання в `estimate_wattage`. Чому саме такі?_
> - _Прочитай `compat_query.py` і поясни собі, чому це попередній фільтр, а не заміна рушію._
> - _Спробуй зламати permissions руками (чужа приватна збірка, чужий коментар, чуже замовлення)._
> - _Що ти змінив би в дизайні і чому (наприклад, AI-порадник як Celery-задача з polling замість синхронного запиту)._

## 4. Чого AI не зробив або не зміг перевірити

- **Docker Compose локально не запускався**, бо на машині розробки немає Docker. Його перевіряє GitHub Actions: збирає образи, піднімає весь стек із 7 сервісів і робить smoke-тест. Перший прогін CI після пушу був зеленим з першої спроби. Продакшн-оверлей (`docker-compose.prod.yml` з Caddy і HTTPS) в CI не запускається, бо для сертифіката потрібен справжній домен.
- **Шлях із Claude API** протестовано скриптованим фейковим клієнтом (форма запиту, відхилення невалідної збірки, fallback при відмові). Реального виклику з ключем не було.
- **Stripe** протестовано моками (підпис вебхука, обробка події). Реальної тестової оплати не було.
- **Деплой** не виконано: інструкцію й конфіги для VPS підготовлено (`docs/DEPLOY.md`), сервер і домен власник проєкту обере сам.
- **Умови використання hotline.ua.** robots.txt дозволяє ботам заходити на сторінки товарів, і бот поводиться чемно, але умови використання сайту я не читав. Перед публічним деплоєм їх варто перевірити або перейти на офіційний партнерський фід. Фото товарів належать виробникам і показуються з посиланням на джерело.
- **Умови використання hotline прочитано лише наприкінці, і це помилка AI.** Увесь збір даних будувався на тому, що robots.txt дозволяє сторінки товарів і не забороняє `/svc/`. Але [угода користувача](https://hotline.ua/ua/page/user_agreement/) прямо забороняє парсинг без письмового дозволу (п. 6.4) і копіювання матеріалів без письмової згоди (п. 2.4). robots.txt — технічна підказка для ботів, а не дозвіл, і AI мав перевірити угоду до першого запиту, а не після. Висновок: збір даних вимкнено перемикачем `MARKET_FETCH_ENABLED` (за замовчуванням 0), дані, зібрані під час розробки, лишаються лише локально й не публікуються, адміністрації hotline надіслано запит на дозвіл. Внутрішній GraphQL для історії цін (`/svc/frontend-api/graphql`) AI знайшов у JS-бандлі й підключив за згодою власника проєкту, так само не звіривши це з угодою.
- **Посилання на hotline** для 65 деталей знайдено через веб-пошук, 8 недоступних моделей замінено найближчими аналогами. Усі посилання перевірено парсером (назва на сторінці збігається), але специфікації замінених моделей (довжина відеокарт тощо) варто звірити з сайтами виробників.

> _Після перевірки цих пунктів онови розділ: що запрацювало одразу, що ні і що довелося виправити._
